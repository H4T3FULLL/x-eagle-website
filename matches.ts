import type {NextApiRequest,NextApiResponse} from 'next';import {getUpcomingMatches} from '../../lib/db';export default async function handler(req:NextApiRequest,res:NextApiResponse){try{const matches=await getUpcomingMatches();res.status(200).json({matches});}catch(e){console.error(e);res.status(500).json({error:'Failed to fetch matches'});}}