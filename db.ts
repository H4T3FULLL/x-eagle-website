import mysql from 'mysql2/promise';
let pool:null|mysql.Pool=null;export function getPool(){if(!pool){pool=mysql.createPool({host:process.env.DB_HOST,user:process.env.DB_USER,password:process.env.DB_PASSWORD,database:process.env.DB_NAME,port:process.env.DB_PORT?Number(process.env.DB_PORT):3306,waitForConnections:true,connectionLimit:10,queueLimit:0});}return pool;}export async function getUpcomingMatches(){const p=getPool();const [rows]=await p.query(`SELECT id, opponent, event_name, match_date, start_time, location, result, score, vod_url FROM matches WHERE match_date >= CURDATE() ORDER BY match_date, start_time LIMIT 100`);return rows as any[];}