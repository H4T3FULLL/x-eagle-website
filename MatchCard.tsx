type Match={id:number;opponent:string;event_name?:string;match_date:string;start_time:string;location:string;result?:string;score?:string;vod_url?:string;};export default function MatchCard({m}:{m:Match}){return(<div className='card'><div className='flex items-center justify-between'><div><h3 className='text-xl font-semibold'>vs {m.opponent}</h3><p className='text-sm text-gray-400'>{m.match_date} • {m.start_time} • {m.location}{m.event_name?` • ${m.event_name}`:''}</p></div><div className='text-right'>{m.result&&<div className='text-sm'>Result: {m.result}{m.score?` (${m.score})`:''}</div>}{m.vod_url&&<a className='navlink text-sm' href={m.vod_url} target='_blank'>VOD</a>}</div></div></div>);}