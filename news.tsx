import {GetStaticProps} from 'next';import {getAllPostsMeta} from '../lib/cms';import NewsCard from '../components/NewsCard';export const getStaticProps:GetStaticProps=async()=>{const posts=await getAllPostsMeta();return{props:{posts}}};export default function News({posts}:any){return(<main className='section'><div className='container'><h1 className='text-3xl font-bold mb-6'>News & Updates</h1><div className='grid md:grid-cols-2 gap-6'>{posts.map((p:any)=>(<NewsCard key={p.slug} slug={p.slug} title={p.title} date={p.date} excerpt={p.excerpt}/>))}</div></div></main>);}