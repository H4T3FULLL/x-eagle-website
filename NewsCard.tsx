import Link from 'next/link';type Props={slug:string;title:string;date:string;excerpt?:string;};export default function NewsCard({slug,title,date,excerpt}:Props){return(<Link href={`/news/${slug}`} className='card block hover:border-zinc-600'><h3 className='text-xl font-semibold'>{title}</h3><p className='text-sm text-gray-400 mt-1'>{date}</p>{excerpt&&<p className='mt-3 text-gray-300'>{excerpt}</p>}</Link>);}