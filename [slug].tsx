import {GetStaticProps,GetStaticPaths} from 'next';import {getAllPostsMeta,getPostBySlug} from '../../lib/cms';export const getStaticPaths:GetStaticPaths=async()=>{const posts=await getAllPostsMeta();return{paths:posts.map((p:any)=>({params:{slug:p.slug}})),fallback:false}};export const getStaticProps:GetStaticProps=async({params})=>{const slug=params?.slug as string;const post=await getPostBySlug(slug);return{props:{post}}};export default function PostPage({post}:any){return(<main className='section'><div className='container'><h1 className='text-3xl font-bold mb-2'>{post.meta.title}</h1><p className='text-sm text-gray-400 mb-6'>{post.meta.date}</p><article className='prose prose-invert max-w-none' dangerouslySetInnerHTML={{__html:post.html}}/></div></main>);}